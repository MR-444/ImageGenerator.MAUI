ImageGenerator.MAUI — Open Work Queue
=====================================

Last updated: 2026-06-16. v1.9.1 RELEASED (tag v1.9.1 on 542e601). This file holds ONLY the
open backlog. Forensics of completed work live in git history (`git log <prev-tag>..`) and in
~/.claude memory; the latest full audit is documents/audit/csharp-audit-2026-06-16.md.

  [ ] = open      [~] = in progress (commit not yet on master)


PENDING VERIFY (shipped, awaiting user UI check)
------------------------------------------------
[ ] AI caption RESTYLE UI-verify: on the Mutation page, AI mode → pick a saved style ("Saved style"
    picker) → confirm the N variants render coherently in that style (whole-scene restyle, not just
    the art_style field). AI round-trip itself already user-verified (Local good / Sonnet + Opus very
    good); this is the one outstanding manual check from the pin-a-style slice (commit 13426eb).


AUDIT FOLLOW-UPS — documents/audit/csharp-audit-2026-06-16.md (very good health; no Critical)
--------------------------------------------------------------------------------------------
[x] F1 (High) — FIXED, committed f8cafbc: IdeaToPromptViewModel now owns a per-build
    CancellationTokenSource, threads the token into BuildProseAsync/BuildJsonAsync, and exposes a
    CancelCommand (+ "Cancel" button on IdeaToPromptPage, visible while busy). OperationCanceledException
    → neutral "Build cancelled." status; prose from pass 1 stays usable. +1 test (cancellation), 8/8 green.
[x] F2 (Med) — FIXED: MutationEngineViewModel.Initialize now sets IsAiMode = IsBreedMode, so an ordinary
    visit resets to the free deterministic engine (a prior Breed flow no longer silently leaves the
    singleton VM on the paid LLM path). +1 regression test, 35/35 MutationEngineViewModelTests green.
[x] F3 (Med) — DECIDED: keep the code, fix the doc. The greedy best-fit `continue` is INTENTIONAL and
    pinned by DescBudgetTests.Fit_GreedilySkipsTooBigPhrase_ButTakesSmallerLaterOne (a too-big high-tier
    phrase is skipped, a smaller low-tier one back-fills). `break` would reverse a named test + drop
    ornament detail. Reworded the Fit/class XML doc to describe best-fit-within-priority + added a guard
    comment at the `continue` so it isn't "fixed" back to `break`. Doc-only; 11/11 DescBudgetTests green.
[x] Polish bundle (Minor) — FIXED: F6 MutationEngineViewModel.InitializeFrom now re-rolls Seed on a
    genuinely new base (same-base early-return still preserves it across nav); F7 MutationLibrary ctor
    defensive-copies its 4 collections (+isolation test); F4 both transports throw HttpRequestException
    (carrying HttpStatusCode) on non-2xx, keeping InvalidOperationException only for a malformed 2xx body.
    1167 tests green, 0 warnings. (F5/F8/F9/F10/F11 = no-action per the report; do NOT re-discover as new.)


ROADMAP / OUTLOOK — recorded forward plan (each = its own future task)
---------------------------------------------------------------------
Respect scope rules (no multi-image-per-click; no kontext/negative_prompt/A1111-in-saved-files).
[ ] One-click upscale of a result — route a finished image through an existing provider (a Replicate
    upscaler model, or a ComfyUI upscale workflow). New operation type, reuses dispatch/provider plumbing.
[ ] Saved parameter presets — name + recall a model+params combo.

NOT selected (record so they aren't re-pitched as new): gallery delete/search/favorites; compare/A-B
view; seed-variation "more like this" (only OK as a sequential batch, never multi-output).


WATCH ITEM
----------
[ ] Ghost Generate activation (confirmed twice 2026-06-10, none since the forensics landed). If a job
    card appears without a click: app.log "Generate activated" lines classify pointer/keyboard/
    programmatic; JobCount=2 after one click = smoking gun; fix = move focus off the button after click.


INERT UNTIL THE USER ASKS (project memory: don't suggest unused features)
-------------------------------------------------------------------------
[ ] kontext (image-prompt for kontext models) — descriptor pattern, near-zero structural impact.
[ ] negative_prompt for Pollinations flux/zimage — same.
[ ] Pollinations image-input — descriptors inherit ImagePrompt:false; API supports input images
    (documents/Pollinations API Reference.html). Gap: per-model ImagePrompt:true, BuildUrl image= param
    (encoding unknown: GET-only API vs the app's base64), and MaxImageInputs > 0 where supported.


SCOPE GUARDS (standing, do not violate)
---------------------------------------
One image per Generate click IS the architecture (no count box, no multi-output). No kontext /
negative_prompt / A1111-chunk-in-SAVED-files unless explicitly requested (CivitAI POSTING via structured
meta IS a built feature). HARD: no Python anywhere in the mutation engine — schema-validity via the
existing C# serializer + validator only.
