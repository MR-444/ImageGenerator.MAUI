ImageGenerator.MAUI — Open Work Queue
=====================================

Last updated: 2026-06-18 (cut release v2.0.1; 1219 tests green, 0 warnings).
v2.0.1 RELEASED (NEW tag v2.0.1 on 680b455, marked Latest) — a patch shipping the two unreleased
Windows-standards/data-path commits on top of v2.0.0: b56886f (one configurable Emberforge root;
app.log -> %LOCALAPPDATA%\Emberforge\app.log) + a837a58 (publisher User Name -> MR-444). Release-prep
commit 680b455 bumped Version 2.0.0->2.0.1 / ApplicationVersion 21->22 and fixed the stale README
app.log line. Migration is DOCUMENT-ONLY (notes at documents/release-notes-v2.0.1.md). Full forensics
live in git history (`git log v2.0.1..`) and in ~/.claude memory ([[project_data_layout_emberforge]],
[[reference_audit_2026_06_16]], [[reference_ollama_local_tier]], [[reference_mutation_prompt_craft]]).

  [ ] = open      [~] = in progress (commit not yet on master)


SESSION 2026-06-16 — AUDIT + AI MUTATION QUALITY (all committed to master, NOT released)
----------------------------------------------------------------------------------------
Commits in order:
  - Whole-app audit documents/audit/csharp-audit-2026-06-16.md (very good health, no Critical) + ALL
    follow-ups fixed: F1 f8cafbc (IdeaToPrompt cancellation), F2 b513ad2 (reset IsAiMode on ordinary
    visit), F3 233def9 (DescBudget doc — greedy best-fit is intentional), F4/F6/F7 c3ed11b (polish),
    docs 0e7edcf. F5/F8-F11 = no-action by design. See [[reference_audit_2026_06_16]].
  - Local-LLM timeout fix 46c54a1: OllamaChatTransport → NATIVE /api/chat with think:false + num_ctx=8192
    + fence-strip. Root cause was THINKING mode (minutes/call → 5-min timeout → ~2 of 4 variants). USER-
    VERIFIED "blindingly fast". Cold model load slow only the FIRST time (OS page cache after).
  - Local sampling 60f1828 (temperature=1.0, top_p=0.95) for more variant spread.
  - Per-index creative lens 0acebf9 + lens-DEFERS-to-steer 6f5b9f8: a blind call can't "differ from
    siblings", so each index gets a concrete lens (time/season/mood/composition/narrative/focal); when a
    steer is present it DOMINATES and the lens steps aside. See [[reference_mutation_prompt_craft]].
  - HLD=overview + detail-in-bbox'd-elements 233b8bf, then forced lean-HLD + bbox-on-EVERY-element 812015e.
    Dodges the Ideogram OPEN-WEIGHTS checkpoint's over-eager block on holistic phrasing (content inside a
    bbox'd element is DECOUPLED and renders even when the same words in high_level_description get gated).
    USER-VERIFIED: no blocked images; lean HLDs + bboxes present in output.
  - Style-library honest names 09c4e71: gouache/anime → real clean styles (were botanist samples),
    density → "vignette" (real photo edge-darken effect), NEW "occult_botanical" preserves the look.
    SEED + the user's LIVE library (Pictures\...\mutation-library\style-fragments.json) both migrated.

MODEL NOTES (local Ollama tier — see [[reference_ollama_local_tier]]): qwen3.6:35b = faithful (preserves
the pinned style, follows structure); Qwen3-Next-80B-A3B-Instruct = drifts/over-associates ("3am → bed",
restyles off the lock, dropped bboxes). All installed models are thinking-capable. 35B is the safer bet.


PENDING VERIFY (shipped this session, awaiting more user eyeballing)
-------------------------------------------------------------------
[ ] BBOX-DECOUPLE reliability: only ONE clean no-block run confirmed. Re-run a few previously-blocked
    prompts on BOTH qwen3.6:35b and the 80B; confirm lean-HLD + every-element-bbox holds and blocks stay
    gone (the 80B ignored structure once — 35B safer).
[ ] AI caption RESTYLE UI-verify: AI mode → pick a saved style → variants render coherently in that style
    (whole-scene restyle). AI round-trip already verified; this is the pin-a-style check (commit 13426eb).
[ ] Style-rename sanity: pin gouache (real gouache, no flora), occult_botanical (flowers on purpose),
    vignette (darkened-edge photo) — confirm names now match output.


ROADMAP / OUTLOOK — recorded forward plan (each = its own future task)
---------------------------------------------------------------------
Respect scope rules (no multi-image-per-click; no kontext/negative_prompt/A1111-in-saved-files).
[ ] One-click upscale of a result — route a finished image through an existing provider (a Replicate
    upscaler model, or a ComfyUI upscale workflow). New operation type, reuses dispatch/provider plumbing.
[ ] Saved parameter presets — name + recall a model+params combo.
[ ] Auto-fill missing REQUIRED node inputs from /object_info (ComfyUI resilience). The app submits frozen
    API-format workflows; when ComfyUI adds a new required input to a node, the GUI silently fills its
    default but the API does NOT, so old saved workflows fail validation and the render dies with a silent
    "no output image". Already bit us once: ComfyUI 0.25.0 added required `multiple` to ResolutionSelector
    (fixed by hand in the sample, commit 5e6a638; user's own wf patched outside the repo). Fix: right before
    submit, GET /object_info once and for every node insert any missing required input using its schema
    default — mirrors what the GUI does, immunizes against this whole class of break. Catch:
    ComfyUiWorkflowPatcher is a PURE OFFLINE transform, so feed object_info IN from the service layer (which
    already talks to ComfyUI) rather than having the patcher make the network call. See
    [[reference_comfyui_resolutionselector_multiple]].

NOT selected (record so they aren't re-pitched as new): gallery delete/search/favorites; compare/A-B
view; seed-variation "more like this" (only OK as a sequential batch, never multi-output).


WATCH ITEM
----------
[ ] Ghost Generate activation (confirmed twice 2026-06-10, none since the forensics landed). If a job
    card appears without a click: app.log "Generate activated" lines classify pointer/keyboard/
    programmatic; JobCount=2 after one click = smoking gun; fix = move focus off the button after click.


INERT UNTIL THE USER ASKS (project memory: don't suggest unused features)
-------------------------------------------------------------------------
[ ] kontext (image-prompt for kontext models) — descriptor pattern, near-zero structural impact.
[ ] negative_prompt for Pollinations flux/zimage — same.
[ ] Pollinations image-input — descriptors inherit ImagePrompt:false; API supports input images
    (documents/Pollinations API Reference.html). Gap: per-model ImagePrompt:true, BuildUrl image= param
    (encoding unknown: GET-only API vs the app's base64), and MaxImageInputs > 0 where supported.


SCOPE GUARDS (standing, do not violate)
---------------------------------------
One image per Generate click IS the architecture (no count box, no multi-output). No kontext /
negative_prompt / A1111-chunk-in-SAVED-files unless explicitly requested (CivitAI POSTING via structured
meta IS a built feature). HARD: no Python anywhere in the mutation engine — schema-validity via the
existing C# serializer + validator only.
